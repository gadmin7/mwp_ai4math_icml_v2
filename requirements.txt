# Deliberately loose. The JarvisLabs image ships a torch built against its CUDA
# driver -- pinning torch here would force a multi-GB reinstall and risk breaking
# that match. src/pipeline.py::build_training_args adapts to whichever
# transformers API is present (eval_strategy vs evaluation_strategy,
# warmup_ratio vs warmup_steps), so no upper caps are needed.
torch>=2.3
transformers>=4.44
peft>=0.12
bitsandbytes>=0.43.1
accelerate>=0.33
datasets>=2.20
huggingface_hub>=0.24
safetensors>=0.4.3
pandas>=2.2
numpy>=1.26
PyYAML>=6.0
